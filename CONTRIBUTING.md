# 贡献指南

欢迎加入 Jev 海龟汤 🐢

这个项目的"内容"和"代码"两条路都可以参与。

---

## 🧩 投稿一题海龟汤（最受欢迎）

这是本项目最常见的贡献方式。投稿无需懂代码。

1. 进入 [Issues](../../issues) → **New issue** → **新题投稿**。
2. 按模板填：
   - **汤面**：玩家读到的故事。离奇、有画面感、信息密度高。
   - **汤底**：完整真相，必须能闭环解释汤面的每一处异常。
   - **三条提示**：三个不同角度（不是阶梯式「更明显」），卡住的玩家看到任意一条都能继续推。
3. 提交后等审核。管理员会在几个工作日内通过 / 驳回。通过后会进入公开题库，作者署名 `author_name`。

### 投稿守则

- 字数：标题 ≤ 30，汤面 ≤ 500，汤底 ≤ 1500，每条提示 ≤ 100。
- 内容：
  - 不含真实人物、地点、事件。
  - 不含种族 / 性别 / 宗教 / 性向等敏感设定。
  - 暴力 / 自残 / 虐待必须止于推断，不做过程描写。
- 改写自经典题目的，请在 `data/library-sources.md` 里登记来源链接。

---

## 💻 改代码

1. Fork → 新建分支（`feat/...` / `fix/...` / `docs/...`）。
2. 本地起两个服务：
   ```bash
   pnpm install
   cp .env.example .env.local
   pnpm dev                              # 前端 :3000
   pnpm --dir worker db:migrate:local    # 首次
   pnpm --dir worker dev                 # 后端 :8787
   ```
3. 详细约定见 [AGENTS.md](./AGENTS.md)（项目导览、关键约定）和 [`worker/README.md`](./worker/README.md)。
4. 提交前 `pnpm --recursive exec tsc --noEmit`。
5. 提 PR，模板会引导你写改动说明 + 验证步骤。

### PR 准则

- 一类改动一个 PR；新功能请先开 Issue 讨论。
- 保持改动小而聚焦，避免顺手改无关代码。
- UI 改动附图（手机模拟器或真机截图）。
- 不要提交密钥、不要提交 `.env.local` / `.dev.vars`。

---

## 🐛 报告 Bug

[Issues](../../issues) → **New issue** → **Bug 报告**。请附：

- 复现步骤（最好用 1/2/3 列出）
- 期望 vs 实际
- 浏览器 + 系统版本
- 控制台 / Network 报错截图

---

## 💡 提建议

[Issues](../../issues) → **New issue** → **功能建议**。一句话讲清楚要解决什么问题、为什么重要、可以怎么实现。

---

## 社区准则

请阅读并遵守 [CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)。维护者保留对不当行为的处理权。

---

## 许可协议

向本项目投稿即表示你同意以 **GPL-3.0** 协议发布你的贡献。详见 [LICENSE](./LICENSE)。