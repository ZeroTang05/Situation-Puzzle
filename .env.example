# 环境变量模板（复制为 .env.local 本地开发 / .env.production 服务器部署）。
# 所有密钥不入库；必需项缺失时对应进程启动失败（fail-fast）。

# ---------- 基础 ----------
# NODE_ENV=development
# PORT=8080
# 对外完整地址（公网入口，含协议；better-auth 用它校验前端来源）
# PUBLIC_BASE_URL=http://localhost:5173
# DATABASE_URL=postgresql://jev:jev@localhost:5432/jev

# ---------- 会话与令牌（各 32+ 字节随机串，openssl rand -hex 32） ----------
# AUTH_SECRET=
# SOLO_TOKEN_SECRET=
# 可选：实时票据独立密钥，缺省从 AUTH_SECRET 派生
# REALTIME_TICKET_SECRET=

# ---------- 邮件（Email OTP 必需） ----------
# 通道显式二选一：resend=生产（HTTP API）；smtp=本地联调（投递给 dev-mailsink 收信台）
# MAIL_TRANSPORT=resend
# Resend 通道：
# RESEND_API_KEY=re_xxxxxxxx
# SMTP 通道：
# SMTP_HOST=
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=Jev <no-reply@example.com>

# ---------- Google OAuth（可选：未配置时仅邮箱登录） ----------
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# 境内服务器出站中继（内部 oauth-relay，Deno Deploy）：token 兑换与 userinfo
# 经中继转发（专用路径 + X-Relay-Token 共享密钥）；授权跳转仍由用户浏览器直连
# Google。海外服务器两项都留空即直连。密钥须与中继部署的 RELAY_SHARED_SECRET 一致。
# GOOGLE_OAUTH_PROXY_BASE_URL=https://oauth-relay.xiaobaozi.cn
# GOOGLE_OAUTH_PROXY_SHARED_SECRET=

# ---------- Jev 判题（OpenCode Zen SystemOne） ----------
# OPENCODE_API_KEY=
# JEV_BASE_URL=https://opencode.ai/zen/v1/systemone
# JEV_MODEL=jev-1.13
# JEV_THRESHOLD=0.5
# JEV_LANGUAGE=zh
# JEV_PROMPT_VERSION=v1-2026-09

# ---------- 单人并发预算（每进程，首轮 2） ----------
# SOLO_JEV_CONCURRENCY=2

# ---------- 微信支付（商户开通前保持留空：收费入口关闭） ----------
# WECHAT_PAY_MCHID=
# WECHAT_PAY_APPID=
# WECHAT_PAY_SERIAL=
# WECHAT_PAY_PRIVATE_KEY_PATH=
# WECHAT_PAY_API_V3_KEY=
# WECHAT_PAY_NOTIFY_URL=
# WECHAT_PAY_PLATFORM_PUBLIC_KEY_PATH=

# ---------- Docker Compose 专用（容器内自动迁移、内部连接串） ----------
# DOMAIN=puzzle.example.com
# POSTGRES_USER=jev
# POSTGRES_PASSWORD=
# POSTGRES_DB=jev
