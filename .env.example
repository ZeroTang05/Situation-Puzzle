# Cloudflare Worker 的完整公开地址，必须以 https:// 开头，例如 https://situation-puzzle-api.<你的账户>.workers.dev
NEXT_PUBLIC_API_URL=
# 本地题目调用 Jev 时使用的服务端密钥；只保存在服务端环境变量中。
OPENCODE_API_KEY=
# 管理员页面的服务端密码，必须与 Worker 的 ADMIN_TOKEN 完全一致；用户名固定为 admin。
ADMIN_TOKEN=
